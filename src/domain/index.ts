/**
 * Dominio compartido de la aplicación generada por KOSMO.
 *
 * Las entidades de negocio, tipos comunes y operaciones compartidas entre múltiples
 * características se ubican en este directorio (ej. `src/domain/appointments/`, `src/domain/expenses/`).
 *
 * Los slices en `src/features/<slug>/` pueden importar desde `src/domain/` para
 * reutilizar la persistencia y reglas de entidades comunes sin acoplarse directamente entre sí.
 */
export {};
